# ── 素材（随本仓库分发，法律声明见 NOTICE.md）────────────────────────────
#
#   assets/voice/            80 条 .opus，2.55 MB —— 角色语音
#   preset/herta.patch.yml   内含 HertaBio.txt 人格正本逐字
#   icon.png                 插件管理页的图标（取自 Herta 上游桌面应用，缩放重编码）
#
# （0.1.7-rc.2 之前这一项叫 `preset/agent.cordis.yml`；DSH 换掉 preset 载体后
#   改名成 `preset/herta.patch.yml`，内容仍是同一份人格语料。）
#
# 这三样不在 Herta 上游的 MIT 范围内（见 THIRD-PARTY.md），现按
# 《崩坏：星穹铁道》同人衍生作品创作指引 V2.0 第三条放置法律声明后收录，
# 声明原文见 NOTICE.md。收录不等于授权：素材权利仍归米哈游及各自所有者，
# 使用仅限非商业用途。
#
# 若米哈游要求移除，删掉这三项即可（见 NOTICE.md 末节）。

# ── 安装备份：含本机 home / profile 路径，与发布无关 ──────────────────────
install-backups/

# ── 构建与运行残留 ────────────────────────────────────────────────────────
node_modules/
*.log
.DS_Store
Thumbs.db
